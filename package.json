{
  "name": "re4r-hexagon-puzzle",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel index.html",
    "clean": "rimraf dist",
    "build": "npm run clean && parcel build index.html",
    "deploy": "npm run build && git subtree push --prefix dist origin gh-pages"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.8.3",
    "rimraf": "^5.0.0"
  }
}
